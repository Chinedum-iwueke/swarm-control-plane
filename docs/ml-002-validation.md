# ML-002 Validation

ML-002 is complete when:

- the schema and migration install from the previous production head;
- a pipeline binds an immutable DATA-002 build and compatible DISC-003 program;
- label end-time, purge, embargo, overlap weighting and train-only fit are explicit;
- adversarial tests reject temporal leakage and lineage drift;
- materialization requires an identical independent rebuild digest;
- a live VM2-backed pilot registers and exactly retrieves both records;
- retained evidence proves the service has no action or capital authority.
