# ALPHA-003 validation

- Hermes schemas require an ordered immutable execution window for the v2 protocol.
- Stage receipts must match the leased stage, campaign, question, dataset and source
  commit.
- Unsupported questions materialize a founder-approved bounded engineering mission
  in `bulletproof_bt`.
- Card confirmation and execution are separate digest-bound approvals.
- The dedicated role package is restricted to risk-zero alpha research execution on
  VM1 and has no capital or order authority.
- Targeted backend tests: 21 passed.
- Targeted worker, bootstrap, role-package and systemd tests: 21 passed.
- Bulletproof contract tests: 9 passed.
- Eight-variant classic-engine replay: truth `PASS`; result `negative`; all bundles
  retained; selection audit confirms held-out test data was not consulted.

Production completion requires the merged commits, VM2 API deployment, VM1 v2 agent
activation and live founder approval of the first card and qualified execution plan.

