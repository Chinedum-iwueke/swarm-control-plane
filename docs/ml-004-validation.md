# ML-004 Validation

Validation covers:

- digest-bound ML-003 evaluation and RI-004 dossier dependencies;
- complete, contiguous reliability diagrams and recomputed expected calibration error;
- held-out uncertainty coverage and interval width;
- regime applicability and support bounds;
- explanation fidelity without causal claims;
- mandatory abstention for miscalibration, distribution shift, low support, and excess uncertainty;
- automatic demotion on undercoverage, misleading explanation, or policy mismatch;
- immutable, idempotent registration and exact API replay;
- zero activation, promotion, order, and capital authority.

The live pilot is a deterministic control-path fixture. It validates institutional wiring,
not real-world model skill.
